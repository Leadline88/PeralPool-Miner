[package]
name = "scheduler"
version = "0.1.0"
edition = "2021"

[dependencies]
stats = { path = "../stats" }
devfee = { path = "../devfee" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
